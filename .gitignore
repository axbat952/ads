node_modules/

# Throwaway Chrome profile and screenshots produced by the smoke test
.smoke-profile/
.smoke-*.png

# Local archive of the earlier mitmproxy-based prototype, superseded by the
# extension. Kept out of git on purpose: it has its own toolchain.
_archive-proxy/
